/// # Design
/// Because this must be the equivalent of a flags enum, it defines a set of constants and operators
/// on those constants.
published const value attribute_target
{
    let value: uint64;

/*
    published const Class = 1.bit_shift_left_by(0);
    published const Struct = 1.bit_shift_left_by(1);
    published const Value = 1.bit_shift_left_by(2);
    published const Trait = 1.bit_shift_left_by(3);
    published const Initializer = 1.bit_shift_left_by(4);
    published const Method = 1.bit_shift_left_by(5);
    published const Getter = 1.bit_shift_left_by(6);
    published const Setter = 1.bit_shift_left_by(7);
    published const Property: attribute_target = Getter | Setter;
    published const Function = 1.bit_shift_left_by(8);
    published const Invocable = Initializer | Method | Property | Function;
    published const Field = 1.bit_shift_left_by(9);
*/
}
