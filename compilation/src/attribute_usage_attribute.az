published const struct attribute_usage_attribute
{
    published let valid_targets: attribute_target;
    published let inherited: bool;
    published let allow_multiple: bool;

    published init(self, .valid_targets, /*.inherited*/ .inherited /*= true*/, /*.allow_multiple*/ .allow_multiple /*= false*/ ) { }
}
