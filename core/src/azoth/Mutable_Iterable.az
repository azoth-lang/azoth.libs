// TODO what is the right way to represent this?
public trait Mutable_Iterable[independent T out]
{
    // TODO should iterate over `Var[T]` to allow mutation
    // TODO rename to mutable_values()? make it a property?
    published fn iterate(temp iso self) -> mut Iterator[/*Var[T]*/ T];
}
