// Note: `Key` type must be `independent` otherwise the `key` field always inherits the `const` from this type.
published const struct key_value_pair[independent(shareable) Key out, independent Value out]
{
    published let key: Key;
    published let value: Value;

    published init(self, .key, .value) { }
}
