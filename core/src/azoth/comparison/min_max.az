/// The maximum of two values.
/// # Remarks
/// If the values are equal, the first value will be returned.
// public fn max[const T](a: T, b: T) -> T
//     where T <: Ordered
//     => if a >= b => a else => b;


// public fn max[any T](a: T, b: T, ordering: Ordering[T] = context) -> T
//     => if a >= b => a else => b;

/// The minimum of two values.
/// # Remarks
/// If the values are equal, the first value will be returned.
// public fn min[const T](a: T, b: T) -> T
//     where T <: Ordered
//     => if a <= b => a else => b;

// public fn min[const T](a: T, b: T, ordering: Ordering[T] = context) -> T
//     => if a <= b => a else => b;
