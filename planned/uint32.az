public extend struct uint32
{
    // public const min: uint = 0;
    // public const max: uint = 2^32;

    public operator _.._ (self, limit: uint) -> Range[uint]
        => Range.closed(self, limit);

    public fn display_format(self) -> string
    {
        // ..
    }
}
