// DRAFT

// Note: T = `void` for promises without a value
public trait Awaitable[T out /*= void*/, TThrows out /*= never*/]
{
    // type TAwaiter <: Awaiter[T, TThrows];

    // published get awaiter() -> TAwaiter;
}
