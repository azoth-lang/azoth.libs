// TODO what is the right way to represent this?
public trait Mutable_Iterable[out T]
{
    // TODO should iterate over `iref var T` to allow mutation
    published fn iterate(lent iso self) -> lent mut Iterator[/*iref var*/ T];
}
