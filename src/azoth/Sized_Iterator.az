/// An iterator for a sequence of a known number of items.
public trait Sized_Iterator[T] <: Finite_Iterator[T]
{
    published fn get_remaining_count(self);

    // TODO length size_hint on remaining? (see Rust)

    // TODO next_chunk()? (see Rust)

    // TODO implement operations once `yield` is implemented

    // How
    // published const empty: const SizedIterator[T] = ...;
}
