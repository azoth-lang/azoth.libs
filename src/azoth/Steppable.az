/// Something that has a successor and predecessor
published trait Steppable[TSelf] <: Ordered[TSelf]
{
    // published fn step_forward(self) -> Self?;

    // published fn step_backward(self) -> Self?;
}
