/// A collection is a collection of items that can be iterated.
public trait Collection[T] <: Iterable[T]
{
    /// O(1)
    published fn get_count(self) -> size;

    /// O(1)
    published fn is_empty(self) -> bool
        => .get_count() == 0;

    /// O(1)?
    published fn clear(mut self);
}
