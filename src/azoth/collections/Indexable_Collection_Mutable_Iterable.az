class Indexable_Collection_Mutable_Iterable[T] <: Mutable_Iterable[T]
{
    let collection: xmut Indexable_Collection[T];

    public new(mut self, .collection) { }

    // TODO should iterate over `ref var T` to allow mutation
    published fn iterate(lent xmut self) -> lent mut Iterator[/*ref var*/ T]
        => new Indexable_Collection_Mutable_Iterator[T](.collection);
}
