// Note: `Key` type must be `ind` otherwise the `key` field always inheirts the `const` from this type.
published const copy struct key_valye_pair[Key ind, Value ind]
{
    published let key: Key;
    published let value: Value;

    published init(self, .key, .value) { }
}
