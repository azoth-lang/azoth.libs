published trait Equivalence[T in] <: Partial_Equivalence[T]
// TODO restrict T to `read`, `const`, or `id` (or `temp const`?) i.e. non-mutable?
{
    published fn equal(self, lent first: T, lent second: T) -> bool;
}
