/// The natural ordering for the type `T` as given by its implementation of `Ordered`.
// TODO hide this behind a `natural_order` property on `Ordered`?
public class Natural_Ordering[T in] <: Ordering[T]
    // where T <: Ordered
{
    // TODO single instance per `T`?

    // published fn compare(self, first: T, second: T) -> ordering
    //     => first.compare(second);
}
